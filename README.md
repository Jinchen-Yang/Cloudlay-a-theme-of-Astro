# 我的博客

一个使用 Astro 构建的现代化个人博客系统，包含博客、笔记和文档功能。

## ✨ 功能特点

- 🚀 **高性能** - Astro 默认零 JavaScript，页面加载速度极快
- 📝 **灵活内容** - 支持博客文章、学习笔记和项目文档
- 🎨 **现代化设计** - 简洁优雅的响应式布局
- 📁 **易于组织** - 清晰的目录结构，便于内容管理
- 🔧 **易于扩展** - 模块化的组件和工具库

## 📁 项目结构

```
src/
├── components/              # 可复用的 Astro 组件
│   ├── ItemLink.astro       # 链接项组件
│   ├── Breadcrumb.astro     # 面包屑导航
│   └── Card.astro           # 卡片组件
├── layouts/                 # 页面布局
│   └── BaseLayout.astro
├── pages/                   # 路由页面
│   ├── index.astro          # 首页
│   ├── blog.astro           # 博客列表
│   ├── docs.astro           # 文档
│   ├── notes.astro          # 笔记首页
│   └── notes/               # 笔记路由
│       ├── [category].astro         # 分类页面
│       └── [category]/[slug].astro  # 文章详情
├── content/                 # 内容集合
│   ├── blog/                # 博客文章
│   ├── notes/               # 学习笔记
│   └── config.ts            # 内容配置
├── styles/                  # 全局样式
│   └── global.css           # CSS 变量和全局样式
└── utils/                   # 工具函数
    └── fs.ts                # 文件系统操作
```

## 🚀 快速开始

### 前置要求
- Node.js 16.x 或更高版本
- npm 或 yarn

### 安装依赖
```bash
npm install
```

### 启动开发服务器
```bash
npm run dev
```

访问 `http://localhost:4321` 查看博客。

### 构建生产版本
```bash
npm run build
```

### 预览生产构建
```bash
npm run preview
```

## 📝 使用说明

### 添加博客文章

在 `src/content/blog/` 目录下创建 `.md` 文件：

```markdown
---
title: '文章标题'
description: '文章描述'
author: '作者名称'
date: 2026-01-15
tags: ['标签1', '标签2']
---

# 文章内容

你的 Markdown 内容...
```

### 添加学习笔记

在 `src/content/notes/` 目录下创建文件夹，然后添加 `.md` 文件：

```
src/content/notes/
├── 高等数学/
│   ├── 傅里叶变换.md
│   └── 微积分基础.md
├── 线性代数/
│   └── 矩阵论.md
```

### 页面导航

- **首页** (`/`) - 项目首页
- **博客** (`/blog`) - 所有博客文章
- **笔记** (`/notes`) - 笔记分类
  - `/notes/{category}` - 分类下的所有文件
  - `/notes/{category}/{slug}` - 文件详细内容
- **文档** (`/docs`) - 开发文档

## 🎨 自定义样式

所有颜色和样式通过 CSS 变量管理，编辑 `src/styles/global.css`：

```css
:root {
  --color-primary: #0066cc;        /* 主颜色 */
  --color-primary-hover: #0052a3;  /* 悬停颜色 */
  --color-bg-light: #fafafa;       /* 浅色背景 */
  --color-text-primary: #333;      /* 主文本 */
  /* ... 更多变量 */
}
```

## 📚 技术栈

- **框架**: Astro 5.x
- **样式**: CSS 3 (CSS Variables)
- **内容**: Markdown + YAML Frontmatter
- **解析**: TypeScript, marked

## 🛠️ 可用命令

| 命令 | 说明 |
|------|------|
| `npm run dev` | 启动开发服务器 |
| `npm run build` | 构建生产网站 |
| `npm run preview` | 预览生产构建 |
| `npm run astro add` | 添加集成 |

## 💡 开发建议

### 添加新页面
1. 在 `src/pages/` 创建 `.astro` 文件
2. 导入 `BaseLayout` 组件
3. 编写内容

### 创建新组件
1. 在 `src/components/` 创建 `.astro` 文件
2. 定义 Props 接口
3. 导入使用

### 使用工具函数
```typescript
import { getDirectoryItems, getCategoryFiles } from '../utils/fs';

// 获取目录项
const items = await getDirectoryItems('/path/to/dir');

// 获取分类文件
const files = await getCategoryFiles('/path/to/category');
```

## 📖 进一步了解

- [Astro 官方文档](https://docs.astro.build)
- [Astro 教程](https://docs.astro.build/zh-cn/tutorial/0-introduction/)
- [Markdown 语法](https://www.markdownguide.org/)

---

**祝你开发愉快！** 🚀
