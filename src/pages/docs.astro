---
import BaseLayout from '../layouts/BaseLayout.astro';
---

<BaseLayout title="开发文档">
  <article class="docs">
    <h1>📖 开发文档</h1>
    <p class="intro">完整的项目开发指南和技术文档</p>

    <div class="toc">
      <h3>目录</h3>
      <ul>
        <li><a href="#structure">项目结构</a></li>
        <li><a href="#quickstart">快速开始</a></li>
        <li><a href="#concepts">核心概念</a></li>
        <li><a href="#usage">使用说明</a></li>
        <li><a href="#customize">自定义</a></li>
      </ul>
    </div>

    <h2 id="structure">📁 项目结构</h2>
    <pre><code>src/
├── components/              # 可复用的 Astro 组件
│   ├── ItemLink.astro       # 链接项组件
│   ├── Breadcrumb.astro     # 面包屑导航
│   └── Card.astro           # 卡片组件
├── layouts/                 # 页面布局
│   └── BaseLayout.astro     # 基础布局（包含导航、页脚）
├── pages/                   # 路由页面（自动转换为 URL）
│   ├── index.astro          # 首页 (/)
│   ├── blog.astro           # 博客列表 (/blog)
│   ├── docs.astro           # 文档 (/docs)
│   ├── notes.astro          # 笔记首页 (/notes)
│   └── notes/               # 笔记子路由
│       ├── [category].astro         # 分类页面
│       └── [category]/[slug].astro  # 文章详情
├── content/                 # 内容集合
│   ├── blog/                # 博客文章（Markdown）
│   ├── notes/               # 学习笔记（按分类组织）
│   └── config.ts            # Content Collections 配置
├── styles/                  # 全局样式
│   └── global.css           # CSS 变量和全局样式
└── utils/                   # 工具函数
    └── fs.ts                # 文件系统操作</code></pre>

    <h2 id="quickstart">🚀 快速开始</h2>

    <h3>1. 启动开发服务器</h3>
    <pre><code>npm run dev</code></pre>
    <p>访问 <code>http://localhost:4321</code> 查看你的博客。开发服务器会监听文件变化并自动重新加载。</p>

    <h3>2. 构建生产版本</h3>
    <pre><code>npm run build</code></pre>
    <p>生成优化的生产网站到 <code>dist/</code> 目录。</p>

    <h3>3. 预览生产构建</h3>
    <pre><code>npm run preview</code></pre>
    <p>本地预览生产构建，确保一切正常。</p>

    <h2 id="concepts">💡 核心概念</h2>

    <h3>Astro 组件 (.astro 文件)</h3>
    <ul>
      <li><strong>前置代码栏</strong>：<code>---</code> 之间的代码仅在服务器运行</li>
      <li><strong>模板部分</strong>：HTML 模板，可以使用前置代码中定义的变量</li>
      <li><strong>作用域样式</strong>：<code><style></code> 中的样式仅作用于该组件</li>
      <li><strong>零 JavaScript</strong>：默认不向浏览器发送 JavaScript</li>
    </ul>

    <h3>布局 (Layouts)</h3>
    <ul>
      <li>在 <code>src/layouts/</code> 中定义页面布局</li>
      <li>使用 <code>&lt;slot /&gt;</code> 放置页面内容</li>
      <li>通过 Props 接收页面标题等元数据</li>
    </ul>

    <h3>路由 (Pages)</h3>
    <ul>
      <li><code>src/pages/</code> 中的每个 <code>.astro</code> 文件自动成为一个路由</li>
      <li>文件名映射到 URL 路径</li>
      <li>动态路由使用 <code>[param]</code> 语法</li>
      <li>示例：<code>pages/blog/[slug].astro</code> → <code>/blog/{slug}</code></li>
    </ul>

    <h3>内容集合 (Content Collections)</h3>
    <ul>
      <li>在 <code>src/content/</code> 中组织 Markdown 内容</li>
      <li><code>config.ts</code> 定义每个集合的数据模式</li>
      <li>支持类型检查和验证</li>
      <li>可以查询、排序和过滤内容</li>
    </ul>

    <h2 id="usage">📝 使用说明</h2>

    <h3>添加博客文章</h3>
    <p>在 <code>src/content/blog/</code> 创建 <code>.md</code> 文件：</p>
    <pre><code>---
title: '文章标题'
description: '简短描述'
author: '作者名称'
date: 2026-01-15
tags: ['标签1', '标签2']
---

# 文章内容

你的 Markdown 内容...</code></pre>

    <h3>添加学习笔记</h3>
    <p>在 <code>src/content/notes/</code> 创建文件夹和文件：</p>
    <pre><code>src/content/notes/
├── 高等数学/
│   ├── 傅里叶变换.md
│   └── 微积分基础.md
├── 线性代数/
│   └── 矩阵论.md
└── 编程/
    └── Python 基础.md</code></pre>

    <h3>创建新页面</h3>
    <ol>
      <li>在 <code>src/pages/</code> 创建 <code>.astro</code> 文件</li>
      <li>导入 <code>BaseLayout</code> 组件</li>
      <li>编写页面内容</li>
      <li>保存后自动生成对应 URL</li>
    </ol>

    <h3>创建新组件</h3>
    <ol>
      <li>在 <code>src/components/</code> 创建 <code>.astro</code> 文件</li>
      <li>定义 Props 接口声明组件属性</li>
      <li>在其他组件或页面中导入使用</li>
    </ol>

    <h2 id="customize">🎨 自定义</h2>

    <h3>修改颜色和样式</h3>
    <p>编辑 <code>src/styles/global.css</code> 修改 CSS 变量：</p>
    <pre><code>:root {
  --color-primary: #0066cc;        /* 主颜色 */
  --color-primary-hover: #0052a3;  /* 悬停颜色 */
  --color-bg-light: #fafafa;       /* 浅色背景 */
  --color-bg-lighter: #f5f5f5;     /* 更浅背景 */
  --color-border: #eee;            /* 边框颜色 */
  --color-text-primary: #333;      /* 主文本 */
  --color-text-secondary: #666;    /* 次文本 */
  --color-text-muted: #999;        /* 灰色文本 */
}</code></pre>

    <h3>添加新字体</h3>
    <p>在 <code>src/styles/global.css</code> 中使用 <code>@font-face</code> 或导入 Google Fonts。</p>

    <h3>扩展功能</h3>
    <ul>
      <li>使用 <code>npm run astro add</code> 添加集成（React、Vue、Tailwind 等）</li>
      <li>在 <code>src/utils/</code> 中添加自定义工具函数</li>
      <li>创建新的可复用组件</li>
    </ul>

    <h2>📚 有用资源</h2>
    <ul>
      <li><a href="https://docs.astro.build" target="_blank">Astro 官方文档</a></li>
      <li><a href="https://docs.astro.build/zh-cn/tutorial/0-introduction/" target="_blank">Astro 教程</a></li>
      <li><a href="https://www.markdownguide.org/" target="_blank">Markdown 语法指南</a></li>
      <li><a href="https://docs.astro.build/zh-cn/guides/integrations-guide/" target="_blank">Astro 集成</a></li>
    </ul>

    <h2>🔧 常用命令</h2>
    <table>
      <tr>
        <th>命令</th>
        <th>说明</th>
      </tr>
      <tr>
        <td><code>npm run dev</code></td>
        <td>启动本地开发服务器</td>
      </tr>
      <tr>
        <td><code>npm run build</code></td>
        <td>构建生产网站</td>
      </tr>
      <tr>
        <td><code>npm run preview</code></td>
        <td>本地预览生产构建</td>
      </tr>
      <tr>
        <td><code>npm run astro add</code></td>
        <td>添加 Astro 集成</td>
      </tr>
    </table>

    <p class="footer-note">祝你开发愉快！🚀</p>
  </article>

  <style>
    .docs {
      line-height: 1.8;
    }

    .intro {
      font-size: 1.1rem;
      color: var(--color-text-secondary);
      margin-bottom: 2rem;
    }

    .toc {
      background-color: var(--color-bg-light);
      border-left: 4px solid var(--color-primary);
      padding: 1.5rem;
      margin-bottom: 2rem;
      border-radius: 6px;
    }

    .toc h3 {
      margin-top: 0;
      margin-bottom: 1rem;
    }

    .toc ul {
      list-style: none;
      margin: 0;
      padding: 0;
    }

    .toc li {
      margin-bottom: 0.5rem;
    }

    .toc a {
      color: var(--color-primary);
      text-decoration: none;
    }

    .toc a:hover {
      text-decoration: underline;
    }

    h2 {
      scroll-margin-top: 100px;
      margin-top: 2.5rem;
      margin-bottom: 1rem;
      padding-bottom: 0.5rem;
      border-bottom: 2px solid var(--color-border);
    }

    h3 {
      margin-top: 1.5rem;
      margin-bottom: 0.8rem;
      color: var(--color-text-primary);
    }

    pre {
      background-color: var(--color-bg-lighter);
      border-radius: 6px;
      padding: 1rem;
      overflow-x: auto;
      margin: 1rem 0;
      border-left: 4px solid var(--color-primary);
    }

    code {
      background-color: var(--color-bg-lighter);
      padding: 0.2em 0.4em;
      border-radius: 3px;
      font-family: 'Monaco', 'Menlo', 'Courier New', monospace;
      font-size: 0.9em;
    }

    pre code {
      background-color: transparent;
      padding: 0;
      border-radius: 0;
    }

    ul, ol {
      margin-left: 2rem;
      margin-bottom: 1rem;
    }

    li {
      margin-bottom: 0.5rem;
    }

    table {
      width: 100%;
      border-collapse: collapse;
      margin: 1rem 0;
    }

    th, td {
      border: 1px solid var(--color-border);
      padding: 0.75rem;
      text-align: left;
    }

    th {
      background-color: var(--color-bg-lighter);
      font-weight: 600;
    }

    tr:hover {
      background-color: var(--color-bg-light);
    }

    a {
      color: var(--color-primary);
      text-decoration: none;
    }

    a:hover {
      text-decoration: underline;
    }

    .footer-note {
      margin-top: 2rem;
      text-align: center;
      color: var(--color-text-secondary);
    }
  </style>
</BaseLayout>
