---
import BaseLayout from '../layouts/BaseLayout.astro';
import { getCollection } from 'astro:content';
import type { CollectionEntry } from 'astro:content';

// 获取博客文章并按日期排序
const posts = (await getCollection('blog')).sort(
  (a: CollectionEntry<'blog'>, b: CollectionEntry<'blog'>) => b.data.data.date?.valueOf?.() || 0 - (b.data.data.date?.valueOf?.() || 0)
).slice(0, 20); // 首页显示最新20篇

// 博客主人信息
const authorInfo = {
  name: "Jinchen Yang",
  title: "全栈开发者",
  avatar: "/avatar.jpg",
  bio: "热爱技术的开发者，专注于 Web 开发、自动化和系统架构。",
  tagline: "一个关于代码、学习和生活的博客",
  location: "北京, 中国",
  email: "jinchen@example.com",
  github: "Jinchen-Yang",
  twitter: "jinchen_yang",
  website: "https://example.com"
};

// 统计信息
const stats = {
  posts: posts.length,
  categories: 5,
  tags: 20
};
---

<BaseLayout title="首页">
  <div class="page-wrapper">
    <!-- 左侧悬浮栏 - 博客主人信息 -->
    <aside class="sidebar">
      <div class="author-card">
        <!-- 头像与基本信息 -->
        <div class="author-header">
          <div class="avatar-wrapper">
            <img 
              src={authorInfo.avatar} 
              alt={authorInfo.name}
              class="avatar"
              onerror="this.src='https://via.placeholder.com/150/667eea/ffffff?text=JY'"
            />
            <div class="avatar-glow"></div>
          </div>
          
          <div class="author-info">
            <h1 class="author-name">{authorInfo.name}</h1>
            <p class="author-title">{authorInfo.title}</p>
            <p class="author-tagline">💭 {authorInfo.tagline}</p>
          </div>
        </div>

        <!-- 个人简介 -->
        <div class="author-bio-section">
          <p class="author-bio">{authorInfo.bio}</p>
        </div>

        <!-- 个人信息 -->
        <div class="author-details">
          <a href={`mailto:${authorInfo.email}`} class="detail-link" title="邮件">
            <span class="detail-icon">✉️</span>
            <span>联系我</span>
          </a>
          <a href={authorInfo.website} class="detail-link" target="_blank" rel="noopener noreferrer" title="个人网站">
            <span class="detail-icon">🌐</span>
            <span>网站</span>
          </a>
        </div>

        <!-- 社交链接 -->
        <div class="social-links">
          <a href={`https://github.com/${authorInfo.github}`} 
             class="social-link github" 
             target="_blank" 
             rel="noopener noreferrer"
             title="GitHub"
             aria-label="GitHub">
            <span class="social-icon">🐙</span>
          </a>
          <a href={`https://twitter.com/${authorInfo.twitter}`} 
             class="social-link twitter" 
             target="_blank" 
             rel="noopener noreferrer"
             title="Twitter"
             aria-label="Twitter">
            <span class="social-icon">𝕏</span>
          </a>
          <a href="/blog" 
             class="social-link blog" 
             title="所有文章"
             aria-label="所有文章">
            <span class="social-icon">📝</span>
          </a>
        </div>

        <!-- 统计数据 -->
        <div class="stats-grid">
          <div class="stat-card">
            <span class="stat-number">{stats.posts}</span>
            <span class="stat-label">篇文章</span>
          </div>
          <div class="stat-card">
            <span class="stat-number">{stats.categories}</span>
            <span class="stat-label">个分类</span>
          </div>
          <div class="stat-card">
            <span class="stat-number">{stats.tags}</span>
            <span class="stat-label">个标签</span>
          </div>
        </div>

        <!-- 快速导航 -->
        <div class="quick-nav">
          <a href="/blog" class="nav-button primary">📚 浏览文章</a>
          <a href="/notes" class="nav-button secondary">📖 学习笔记</a>
        </div>
      </div>
    </aside>

    <!-- 中部主要内容区域 -->
    <main class="main-content">
      <!-- 欢迎区域 -->
      <section class="welcome-section">
        <div class="welcome-content">
          <h2 class="welcome-title">✨ 欢迎来到我的博客</h2>
          <p class="welcome-subtitle">分享技术心得、学习笔记和生活感悟的地方</p>
        </div>
      </section>

      <!-- 最新文章列表 -->
      <section class="posts-section">
        <div class="section-header">
          <h2 class="section-title">📝 最新文章</h2>
          <a href="/blog" class="view-all">查看全部 →</a>
        </div>

        <div class="posts-container">
          {posts.length === 0 ? (
            <div class="empty-state">
              <p class="empty-icon">📭</p>
              <p class="empty-message">暂时还没有文章，敬请期待！</p>
            </div>
          ) : (
            posts.map((post) => (
              <article class="post-card">
                <a href={`/blog/${post.slug}`} class="post-link">
                  <div class="post-inner">
                    <!-- 文章标签 -->
                    {post.data.tags && post.data.tags.length > 0 && (
                      <div class="post-tags-row">
                        <span class="post-tag">{post.data.tags[0]}</span>
                      </div>
                    )}

                    <!-- 文章标题与元信息 -->
                    <div class="post-header">
                      <h3 class="post-title">{post.data.title}</h3>
                      <div class="post-meta">
                        <time class="post-date" datetime={post.data.date?.toISOString()}>
                          {post.data.date?.toLocaleDateString('zh-CN', {
                            year: 'numeric',
                            month: 'short',
                            day: 'numeric'
                          })}
                        </time>
                        {post.data.author && (
                          <span class="post-author">by {post.data.author}</span>
                        )}
                      </div>
                    </div>

                    <!-- 文章描述/摘要 -->
                    <p class="post-excerpt">
                      {post.data.description || '这篇文章还没有描述...'}
                    </p>

                    <!-- 底部操作 -->
                    <div class="post-footer">
                      <span class="read-more">继续阅读</span>
                      <span class="arrow">→</span>
                    </div>
                  </div>
                </a>
              </article>
            ))
          )}
        </div>
      </section>
    </main>
  </div>

  <style is:global>
    /* CSS 变量定义 */
    :root {
      --color-primary: #667eea;
      --color-primary-light: #f0f4ff;
      --color-primary-hover: #5568d3;
      --color-secondary: #764ba2;
      --color-success: #48bb78;
      --color-warning: #ed8936;
      --color-danger: #f56565;
      --color-bg: #ffffff;
      --color-bg-secondary: #f8f9fa;
      --color-text-primary: #1a202c;
      --color-text-secondary: #718096;
      --color-text-muted: #a0aec0;
      --color-border: #e2e8f0;
      --color-shadow: rgba(0, 0, 0, 0.1);
      --shadow-sm: 0 1px 3px var(--color-shadow);
      --shadow-md: 0 4px 6px var(--color-shadow);
      --shadow-lg: 0 10px 25px rgba(0, 0, 0, 0.15);
      --shadow-xl: 0 20px 40px rgba(0, 0, 0, 0.2);
      --radius-sm: 6px;
      --radius-md: 12px;
      --radius-lg: 16px;
      --transition-fast: 0.15s ease;
      --transition-base: 0.3s ease;
      --transition-slow: 0.5s ease;
    }

    @media (prefers-color-scheme: dark) {
      :root {
        --color-bg: #0f0f0f;
        --color-bg-secondary: #1a1a1a;
        --color-text-primary: #e2e8f0;
        --color-text-secondary: #a0aec0;
        --color-text-muted: #718096;
        --color-border: #2d3748;
      }
    }

    /* 页面容器 */
    .page-wrapper {
      display: grid;
      grid-template-columns: 320px 1fr;
      gap: 2rem;
      padding: 2rem;
      max-width: 1400px;
      margin: 0 auto;
      min-height: calc(100vh - 180px);
    }

    /* ========== 左侧侧边栏样式 ========== */
    .sidebar {
      position: sticky;
      top: 100px;
      height: fit-content;
    }

    .author-card {
      background: linear-gradient(135deg, #f8f9fa 0%, #ffffff 100%);
      border: 1px solid var(--color-border);
      border-radius: var(--radius-lg);
      padding: 2rem;
      box-shadow: var(--shadow-md);
      backdrop-filter: blur(10px);
      transition: all var(--transition-base);
    }

    .author-card:hover {
      box-shadow: var(--shadow-lg);
      border-color: var(--color-primary);
    }

    /* 头像区域 */
    .author-header {
      text-align: center;
      margin-bottom: 2rem;
    }

    .avatar-wrapper {
      position: relative;
      width: 120px;
      height: 120px;
      margin: 0 auto 1.5rem;
      border-radius: 50%;
      overflow: hidden;
    }

    .avatar {
      width: 100%;
      height: 100%;
      object-fit: cover;
      transition: transform var(--transition-base);
    }

    .avatar-wrapper:hover .avatar {
      transform: scale(1.05);
    }

    .avatar-glow {
      position: absolute;
      inset: -4px;
      border-radius: 50%;
      background: linear-gradient(135deg, var(--color-primary), var(--color-secondary));
      opacity: 0;
      transition: opacity var(--transition-base);
      z-index: -1;
    }

    .author-card:hover .avatar-glow {
      opacity: 0.3;
    }

    .author-name {
      font-size: 1.75rem;
      font-weight: 700;
      margin: 0 0 0.5rem;
      color: var(--color-text-primary);
      background: linear-gradient(135deg, var(--color-primary), var(--color-secondary));
      -webkit-background-clip: text;
      -webkit-text-fill-color: transparent;
      background-clip: text;
    }

    .author-title {
      font-size: 0.95rem;
      color: var(--color-primary);
      font-weight: 500;
      margin: 0 0 0.5rem;
    }

    .author-tagline {
      font-size: 0.9rem;
      color: var(--color-text-secondary);
      margin: 0;
      font-style: italic;
    }

    /* 生物区域 */
    .author-bio-section {
      margin-bottom: 1.5rem;
      padding-bottom: 1.5rem;
      border-bottom: 1px solid var(--color-border);
    }

    .author-bio {
      font-size: 0.95rem;
      color: var(--color-text-secondary);
      line-height: 1.6;
      margin: 0;
      text-align: center;
    }

    /* 详细信息链接 */
    .author-details {
      display: flex;
      gap: 0.75rem;
      margin-bottom: 1.5rem;
    }

    .detail-link {
      flex: 1;
      display: flex;
      align-items: center;
      justify-content: center;
      gap: 0.5rem;
      padding: 0.75rem;
      background-color: var(--color-primary-light);
      border: 1px solid transparent;
      border-radius: var(--radius-md);
      color: var(--color-primary);
      text-decoration: none;
      font-size: 0.9rem;
      font-weight: 500;
      transition: all var(--transition-fast);
      white-space: nowrap;
      overflow: hidden;
      text-overflow: ellipsis;
    }

    .detail-link:hover {
      background-color: var(--color-primary);
      color: white;
      transform: translateY(-2px);
    }

    .detail-icon {
      font-size: 1.1rem;
      flex-shrink: 0;
    }

    /* 社交链接 */
    .social-links {
      display: flex;
      gap: 0.75rem;
      margin-bottom: 1.5rem;
      justify-content: center;
    }

    .social-link {
      display: flex;
      align-items: center;
      justify-content: center;
      width: 44px;
      height: 44px;
      border-radius: 50%;
      background-color: var(--color-bg-secondary);
      color: var(--color-text-primary);
      text-decoration: none;
      transition: all var(--transition-fast);
      border: 1px solid var(--color-border);
    }

    .social-link:hover {
      transform: translateY(-4px) scale(1.1);
      box-shadow: var(--shadow-lg);
    }

    .social-link.github:hover {
      background: linear-gradient(135deg, #333, #555);
      color: white;
      border-color: #333;
    }

    .social-link.twitter:hover {
      background: linear-gradient(135deg, #1da1f2, #1a8cd8);
      color: white;
      border-color: #1da1f2;
    }

    .social-link.blog:hover {
      background: linear-gradient(135deg, var(--color-primary), var(--color-secondary));
      color: white;
      border-color: var(--color-primary);
    }

    .social-icon {
      font-size: 1.3rem;
    }

    /* 统计网格 */
    .stats-grid {
      display: grid;
      grid-template-columns: 1fr 1fr 1fr;
      gap: 1rem;
      margin-bottom: 1.5rem;
      padding-bottom: 1.5rem;
      border-bottom: 1px solid var(--color-border);
    }

    .stat-card {
      display: flex;
      flex-direction: column;
      align-items: center;
      padding: 1rem;
      background-color: var(--color-primary-light);
      border-radius: var(--radius-md);
      transition: all var(--transition-fast);
    }

    .stat-card:hover {
      background-color: var(--color-primary);
      color: white;
      transform: translateY(-2px);
    }

    .stat-number {
      font-size: 1.5rem;
      font-weight: 700;
      color: var(--color-primary);
      margin-bottom: 0.25rem;
    }

    .stat-card:hover .stat-number {
      color: white;
    }

    .stat-label {
      font-size: 0.8rem;
      color: var(--color-text-secondary);
      text-align: center;
    }

    .stat-card:hover .stat-label {
      color: rgba(255, 255, 255, 0.9);
    }

    /* 快速导航 */
    .quick-nav {
      display: flex;
      flex-direction: column;
      gap: 0.75rem;
    }

    .nav-button {
      display: block;
      padding: 1rem;
      border-radius: var(--radius-md);
      text-align: center;
      text-decoration: none;
      font-weight: 600;
      transition: all var(--transition-fast);
      border: none;
      cursor: pointer;
      font-size: 0.95rem;
    }

    .nav-button.primary {
      background: linear-gradient(135deg, var(--color-primary), var(--color-secondary));
      color: white;
      box-shadow: var(--shadow-md);
    }

    .nav-button.primary:hover {
      transform: translateY(-2px);
      box-shadow: var(--shadow-lg);
    }

    .nav-button.secondary {
      background-color: var(--color-bg-secondary);
      color: var(--color-primary);
      border: 2px solid var(--color-primary);
    }

    .nav-button.secondary:hover {
      background-color: var(--color-primary);
      color: white;
      transform: translateY(-2px);
    }

    /* ========== 主要内容区域样式 ========== */
    .main-content {
      display: flex;
      flex-direction: column;
      gap: 2rem;
    }

    /* 欢迎区域 */
    .welcome-section {
      background: linear-gradient(135deg, var(--color-primary), var(--color-secondary));
      border-radius: var(--radius-lg);
      padding: 3rem;
      color: white;
      box-shadow: var(--shadow-lg);
      animation: slideInDown 0.6s ease;
    }

    @keyframes slideInDown {
      from {
        opacity: 0;
        transform: translateY(-20px);
      }
      to {
        opacity: 1;
        transform: translateY(0);
      }
    }

    .welcome-content {
      text-align: center;
    }

    .welcome-title {
      font-size: 2.25rem;
      font-weight: 700;
      margin: 0 0 1rem;
    }

    .welcome-subtitle {
      font-size: 1.1rem;
      opacity: 0.9;
      margin: 0;
    }

    /* 文章列表区域 */
    .posts-section {
      display: flex;
      flex-direction: column;
      gap: 1.5rem;
    }

    .section-header {
      display: flex;
      align-items: center;
      justify-content: space-between;
      margin-bottom: 0.5rem;
    }

    .section-title {
      font-size: 1.75rem;
      font-weight: 700;
      margin: 0;
      color: var(--color-text-primary);
    }

    .view-all {
      color: var(--color-primary);
      text-decoration: none;
      font-weight: 600;
      transition: all var(--transition-fast);
      font-size: 0.95rem;
    }

    .view-all:hover {
      color: var(--color-primary-hover);
      transform: translateX(4px);
    }

    /* 文章容器 */
    .posts-container {
      display: flex;
      flex-direction: column;
      gap: 1.25rem;
    }

    /* 文章卡片 */
    .post-card {
      background: var(--color-bg);
      border: 1px solid var(--color-border);
      border-radius: var(--radius-md);
      transition: all var(--transition-base);
      overflow: hidden;
    }

    .post-card:hover {
      border-color: var(--color-primary);
      box-shadow: var(--shadow-lg);
      transform: translateY(-4px);
    }

    .post-link {
      text-decoration: none;
      color: inherit;
      display: block;
      height: 100%;
    }

    .post-inner {
      padding: 1.75rem;
      display: flex;
      flex-direction: column;
      gap: 1rem;
    }

    /* 文章标签行 */
    .post-tags-row {
      display: flex;
      gap: 0.5rem;
      flex-wrap: wrap;
    }

    .post-tag {
      display: inline-block;
      background: linear-gradient(135deg, var(--color-primary-light), rgba(102, 126, 234, 0.1));
      color: var(--color-primary);
      padding: 0.4rem 0.8rem;
      border-radius: 20px;
      font-size: 0.8rem;
      font-weight: 600;
      border: 1px solid rgba(102, 126, 234, 0.3);
      transition: all var(--transition-fast);
    }

    .post-card:hover .post-tag {
      background: var(--color-primary);
      color: white;
      border-color: var(--color-primary);
    }

    /* 文章头部 */
    .post-header {
      display: flex;
      flex-direction: column;
      gap: 0.75rem;
    }

    .post-title {
      font-size: 1.4rem;
      font-weight: 700;
      color: var(--color-text-primary);
      margin: 0;
      line-height: 1.4;
      transition: color var(--transition-fast);
    }

    .post-card:hover .post-title {
      color: var(--color-primary);
    }

    .post-meta {
      display: flex;
      gap: 1.25rem;
      font-size: 0.85rem;
      color: var(--color-text-muted);
      flex-wrap: wrap;
      align-items: center;
    }

    .post-date {
      display: inline-flex;
      align-items: center;
      gap: 0.3rem;
    }

    .post-date::before {
      content: "📅";
    }

    .post-author {
      display: inline-flex;
      align-items: center;
      gap: 0.3rem;
    }

    .post-author::before {
      content: "✍️";
    }

    /* 文章摘要 */
    .post-excerpt {
      color: var(--color-text-secondary);
      line-height: 1.7;
      margin: 0;
      font-size: 0.95rem;
      display: -webkit-box;
      -webkit-line-clamp: 2;
      -webkit-box-orient: vertical;
      overflow: hidden;
    }

    /* 文章底部 */
    .post-footer {
      display: flex;
      justify-content: space-between;
      align-items: center;
      padding-top: 1rem;
      border-top: 1px solid var(--color-border);
      transition: all var(--transition-fast);
    }

    .read-more {
      color: var(--color-primary);
      font-weight: 600;
      font-size: 0.95rem;
      transition: color var(--transition-fast);
    }

    .arrow {
      color: var(--color-primary);
      font-weight: 600;
      transition: transform var(--transition-fast);
    }

    .post-card:hover .arrow {
      transform: translateX(4px);
    }

    /* 空状态 */
    .empty-state {
      text-align: center;
      padding: 4rem 2rem;
      background: linear-gradient(135deg, var(--color-primary-light), rgba(118, 75, 162, 0.05));
      border-radius: var(--radius-lg);
      border: 2px dashed var(--color-border);
    }

    .empty-icon {
      font-size: 3rem;
      margin-bottom: 1rem;
    }

    .empty-message {
      font-size: 1.1rem;
      color: var(--color-text-secondary);
      margin: 0;
    }

    /* ========== 响应式设计 ========== */

    /* iPad 横屏 (1024px - 768px) */
    @media (max-width: 1024px) {
      .page-wrapper {
        grid-template-columns: 280px 1fr;
        gap: 1.5rem;
        padding: 1.5rem;
      }

      .sidebar {
        top: 80px;
      }

      .author-card {
        padding: 1.5rem;
      }

      .welcome-title {
        font-size: 1.75rem;
      }

      .section-title {
        font-size: 1.5rem;
      }

      .post-title {
        font-size: 1.25rem;
      }
    }

    /* iPad 竖屏 (768px - 769px) */
    @media (max-width: 768px) {
      .page-wrapper {
        grid-template-columns: 1fr;
        gap: 2rem;
        padding: 1rem;
      }

      .sidebar {
        position: static;
        top: auto;
      }

      .author-card {
        display: grid;
        grid-template-columns: 100px 1fr;
        gap: 1.5rem;
        align-items: start;
      }

      .author-header {
        grid-column: 1 / -1;
        margin-bottom: 1rem;
        text-align: left;
        display: flex;
        gap: 1rem;
        align-items: start;
      }

      .avatar-wrapper {
        width: 80px;
        height: 80px;
        margin: 0;
        flex-shrink: 0;
      }

      .author-name {
        font-size: 1.4rem;
        -webkit-text-fill-color: unset;
        background: none;
      }

      .author-title,
      .author-tagline {
        font-size: 0.85rem;
        text-align: left;
      }

      .author-bio-section {
        grid-column: 1 / -1;
        margin-bottom: 1rem;
        padding-bottom: 1rem;
      }

      .author-bio {
        text-align: left;
      }

      .author-details {
        grid-column: 1 / -1;
        margin-bottom: 1rem;
      }

      .detail-link {
        font-size: 0.8rem;
      }

      .social-links {
        grid-column: 1 / -1;
        justify-content: flex-start;
      }

      .stats-grid {
        grid-column: 1 / -1;
        grid-template-columns: 1fr 1fr 1fr;
      }

      .quick-nav {
        grid-column: 1 / -1;
      }

      .welcome-section {
        padding: 2rem;
      }

      .welcome-title {
        font-size: 1.5rem;
      }

      .welcome-subtitle {
        font-size: 1rem;
      }

      .section-title {
        font-size: 1.4rem;
      }

      .post-title {
        font-size: 1.15rem;
      }

      .post-meta {
        font-size: 0.8rem;
        gap: 0.75rem;
      }

      .post-excerpt {
        -webkit-line-clamp: 3;
      }

      .section-header {
        flex-direction: column;
        align-items: flex-start;
        gap: 0.75rem;
      }
    }

    /* 手机端 (小于 480px) */
    @media (max-width: 480px) {
      .page-wrapper {
        padding: 1rem;
        gap: 1.5rem;
      }

      .author-card {
        padding: 1.25rem;
        gap: 1rem;
      }

      .avatar-wrapper {
        width: 70px;
        height: 70px;
      }

      .author-name {
        font-size: 1.25rem;
      }

      .stats-grid {
        grid-template-columns: 1fr 1fr;
        gap: 0.75rem;
      }

      .stat-card {
        padding: 0.75rem;
      }

      .stat-number {
        font-size: 1.3rem;
      }

      .stat-label {
        font-size: 0.75rem;
      }

      .welcome-section {
        padding: 1.5rem;
      }

      .welcome-title {
        font-size: 1.3rem;
        margin-bottom: 0.75rem;
      }

      .welcome-subtitle {
        font-size: 0.95rem;
      }

      .section-title {
        font-size: 1.25rem;
      }

      .post-inner {
        padding: 1.25rem;
        gap: 0.75rem;
      }

      .post-title {
        font-size: 1.05rem;
      }

      .post-excerpt {
        font-size: 0.9rem;
        -webkit-line-clamp: 2;
      }

      .post-meta {
        font-size: 0.75rem;
        gap: 0.5rem;
      }

      .post-footer {
        padding-top: 0.75rem;
      }

      .nav-button {
        padding: 0.875rem;
        font-size: 0.9rem;
      }

      .detail-link {
        padding: 0.6rem;
        font-size: 0.75rem;
      }

      .social-link {
        width: 40px;
        height: 40px;
      }

      .social-icon {
        font-size: 1.1rem;
      }
    }

    /* 深色模式 */
    @media (prefers-color-scheme: dark) {
      .welcome-section {
        background: linear-gradient(135deg, #5568d3, #6c5b95);
      }

      .author-card {
        background: linear-gradient(135deg, #1a1a1a 0%, #242424 100%);
        border-color: #333;
      }

      .author-card:hover {
        border-color: var(--color-primary);
      }

      .post-card {
        background: #1a1a1a;
        border-color: #333;
      }

      .post-card:hover {
        border-color: var(--color-primary);
      }

      .empty-state {
        background: linear-gradient(135deg, rgba(102, 126, 234, 0.1), rgba(118, 75, 162, 0.05));
        border-color: #333;
      }

      .stat-card {
        background-color: rgba(102, 126, 234, 0.1);
      }

      .stat-card:hover {
        background-color: var(--color-primary);
      }

      .nav-button.secondary {
        background-color: #242424;
        border-color: var(--color-primary);
      }

      .nav-button.secondary:hover {
        background-color: var(--color-primary);
        color: white;
      }

      .detail-link {
        background-color: rgba(102, 126, 234, 0.1);
        border-color: rgba(102, 126, 234, 0.3);
      }

      .detail-link:hover {
        background-color: var(--color-primary);
        color: white;
        border-color: var(--color-primary);
      }

      .social-link {
        background-color: #242424;
        border-color: #333;
      }

      .social-link:hover {
        border-color: var(--color-primary);
      }
    }

    /* 动画效果 */
    @keyframes fadeIn {
      from {
        opacity: 0;
      }
      to {
        opacity: 1;
      }
    }

    .post-card {
      animation: fadeIn 0.5s ease;
    }

    .post-card:nth-child(2) {
      animation-delay: 0.1s;
    }

    .post-card:nth-child(3) {
      animation-delay: 0.2s;
    }

    .post-card:nth-child(n+4) {
      animation-delay: 0.3s;
    }
  </style>
</BaseLayout>
