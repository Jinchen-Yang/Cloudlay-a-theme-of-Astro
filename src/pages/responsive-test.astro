---
import BaseLayout from '../layouts/BaseLayout.astro';
---

<BaseLayout title="响应式设计测试">
  <div class="test-container">
    <h1>响应式设计测试</h1>
    <p class="subtitle">测试博客在不同设备上的显示效果</p>

    <div class="device-mockups">
      <!-- 手机设备模拟 -->
      <div class="device-mockup mobile">
        <div class="device-frame">
          <div class="device-screen">
            <div class="screen-content">
              <h3>📱 手机端</h3>
              <p>屏幕宽度: < 768px</p>
              <ul class="features">
                <li>单列布局</li>
                <li>头像和信息堆叠显示</li>
                <li>触摸友好的按钮</li>
                <li>简化导航</li>
              </ul>
            </div>
          </div>
        </div>
        <div class="device-label">手机</div>
      </div>

      <!-- iPad设备模拟 -->
      <div class="device-mockup tablet">
        <div class="device-frame">
          <div class="device-screen">
            <div class="screen-content">
              <h3>📱 iPad端</h3>
              <p>屏幕宽度: 768px - 1024px</p>
              <ul class="features">
                <li>横屏时双列布局</li>
                <li>竖屏时单列布局</li>
                <li>优化的触摸交互</li>
                <li>中等尺寸字体</li>
              </ul>
            </div>
          </div>
        </div>
        <div class="device-label">iPad</div>
      </div>

      <!-- 电脑设备模拟 -->
      <div class="device-mockup desktop">
        <div class="device-frame">
          <div class="device-screen">
            <div class="screen-content">
              <h3>💻 电脑端</h3>
              <p>屏幕宽度: > 1024px</p>
              <ul class="features">
                <li>双列布局（左侧悬浮栏）</li>
                <li>悬停效果</li>
                <li>更大的字体和间距</li>
                <li>完整的社交链接</li>
              </ul>
            </div>
          </div>
        </div>
        <div class="device-label">电脑</div>
      </div>
    </div>

    <div class="breakpoints-info">
      <h2>断点说明</h2>
      <div class="breakpoints-grid">
        <div class="breakpoint-card">
          <div class="breakpoint-header mobile-breakpoint">
            <span class="breakpoint-icon">📱</span>
            <h3>手机端</h3>
          </div>
          <div class="breakpoint-content">
            <p><strong>最大宽度:</strong> 767px</p>
            <p><strong>布局:</strong> 单列</p>
            <p><strong>特点:</strong> 触摸优化，简化界面</p>
          </div>
        </div>

        <div class="breakpoint-card">
          <div class="breakpoint-header tablet-breakpoint">
            <span class="breakpoint-icon">📱</span>
            <h3>平板端</h3>
          </div>
          <div class="breakpoint-content">
            <p><strong>宽度范围:</strong> 768px - 1024px</p>
            <p><strong>布局:</strong> 自适应</p>
            <p><strong>特点:</strong> 横屏双列，竖屏单列</p>
          </div>
        </div>

        <div class="breakpoint-card">
          <div class="breakpoint-header desktop-breakpoint">
            <span class="breakpoint-icon">💻</span>
            <h3>电脑端</h3>
          </div>
          <div class="breakpoint-content">
            <p><strong>最小宽度:</strong> 1025px</p>
            <p><strong>布局:</strong> 双列</p>
            <p><strong>特点:</strong> 悬浮侧边栏，丰富交互</p>
          </div>
        </div>
      </div>
    </div>

    <div class="test-actions">
      <a href="/" class="test-button primary">返回首页</a>
      <a href="/blog" class="test-button secondary">查看博客</a>
    </div>
  </div>

  <style>
    .test-container {
      max-width: 1200px;
      margin: 0 auto;
      padding: 2rem;
    }

    h1 {
      font-size: 2.5rem;
      text-align: center;
      margin-bottom: 1rem;
      color: var(--color-text-primary);
    }

    .subtitle {
      text-align: center;
      color: var(--color-text-secondary);
      margin-bottom: 3rem;
      font-size: 1.1rem;
    }

    /* 设备模拟样式 */
    .device-mockups {
      display: flex;
      justify-content: center;
      gap: 3rem;
      margin-bottom: 4rem;
      flex-wrap: wrap;
    }

    .device-mockup {
      display: flex;
      flex-direction: column;
      align-items: center;
    }

    .device-frame {
      background: #333;
      border-radius: 20px;
      padding: 15px;
      box-shadow: 0 10px 30px rgba(0, 0, 0, 0.2);
      position: relative;
    }

    .mobile .device-frame {
      width: 300px;
      height: 600px;
    }

    .tablet .device-frame {
      width: 400px;
      height: 550px;
    }

    .desktop .device-frame {
      width: 500px;
      height: 350px;
    }

    .device-screen {
      background: white;
      border-radius: 10px;
      height: 100%;
      overflow: hidden;
      display: flex;
      align-items: center;
      justify-content: center;
    }

    .screen-content {
      padding: 2rem;
      text-align: center;
    }

    .screen-content h3 {
      font-size: 1.5rem;
      margin-bottom: 1rem;
      color: var(--color-text-primary);
    }

    .screen-content p {
      color: var(--color-text-secondary);
      margin-bottom: 1.5rem;
    }

    .features {
      text-align: left;
      list-style: none;
      padding: 0;
    }

    .features li {
      padding: 0.5rem 0;
      color: var(--color-text-primary);
      position: relative;
      padding-left: 1.5rem;
    }

    .features li:before {
      content: "✓";
      color: var(--color-primary);
      position: absolute;
      left: 0;
    }

    .device-label {
      margin-top: 1rem;
      font-weight: 600;
      color: var(--color-text-primary);
      font-size: 1.1rem;
    }

    /* 断点信息样式 */
    .breakpoints-info {
      margin-bottom: 4rem;
    }

    .breakpoints-info h2 {
      text-align: center;
      margin-bottom: 2rem;
      font-size: 2rem;
    }

    .breakpoints-grid {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
      gap: 2rem;
    }

    .breakpoint-card {
      background: white;
      border-radius: 12px;
      overflow: hidden;
      border: 1px solid var(--color-border);
      transition: transform 0.3s ease, box-shadow 0.3s ease;
    }

    .breakpoint-card:hover {
      transform: translateY(-5px);
      box-shadow: 0 10px 25px rgba(0, 0, 0, 0.1);
    }

    .breakpoint-header {
      padding: 1.5rem;
      color: white;
      display: flex;
      align-items: center;
      gap: 1rem;
    }

    .mobile-breakpoint {
      background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
    }

    .tablet-breakpoint {
      background: linear-gradient(135deg, #f093fb 0%, #f5576c 100%);
    }

    .desktop-breakpoint {
      background: linear-gradient(135deg, #4facfe 0%, #00f2fe 100%);
    }

    .breakpoint-icon {
      font-size: 2rem;
    }

    .breakpoint-header h3 {
      margin: 0;
      font-size: 1.3rem;
    }

    .breakpoint-content {
      padding: 1.5rem;
    }

    .breakpoint-content p {
      margin: 0.75rem 0;
      color: var(--color-text-secondary);
    }

    .breakpoint-content strong {
      color: var(--color-text-primary);
    }

    /* 测试按钮样式 */
    .test-actions {
      display: flex;
      justify-content: center;
      gap: 1rem;
      margin-top: 3rem;
    }

    .test-button {
      padding: 1rem 2rem;
      border-radius: 8px;
      text-decoration: none;
      font-weight: 600;
      transition: all 0.3s ease;
      border: none;
      cursor: pointer;
      font-size: 1rem;
    }

    .test-button.primary {
      background-color: var(--color-primary);
      color: white;
    }

    .test-button.primary:hover {
      background-color: var(--color-primary-hover);
      transform: translateY(-2px);
      box-shadow: 0 5px 15px rgba(0, 102, 204, 0.3);
    }

    .test-button.secondary {
      background-color: transparent;
      color: var(--color-primary);
      border: 2px solid var(--color-primary);
    }

    .test-button.secondary:hover {
      background-color: var(--color-primary);
      color: white;
      transform: translateY(-2px);
    }

    /* 响应式设计 */
    @media (max-width: 1024px) {
      .device-mockups {
        gap: 2rem;
      }

      .mobile .device-frame {
        width: 250px;
        height: 500px;
      }

      .tablet .device-frame {
        width: 350px;
        height: 480px;
      }

      .desktop .device-frame {
        width: 450px;
        height: 300px;
      }
    }

    @media (max-width: 768px) {
      .test-container {
        padding: 1rem;
      }

      h1 {
        font-size: 2rem;
      }

      .device-mockups {
        flex-direction: column;
        align-items: center;
      }

      .breakpoints-grid {
        grid-template-columns: 1fr;
      }

      .test-actions {
        flex-direction: column;
        align-items: center;
      }

      .test-button {
        width: 100%;
        max-width: 300px;
        text-align: center;
      }
    }

    @media (max-width: 480px) {
      .mobile .device-frame {
        width: 200px;
        height: 400px;
      }

      .tablet .device-frame {
        width: 280px;
        height: 380px;
      }

      .desktop .device-frame {
        width: 320px;
        height: 220px;
      }

      .screen-content {
        padding: 1rem;
      }

      .screen-content h3 {
        font-size: 1.2rem;
      }
    }

    /* 深色模式支持 */
    @media (prefers-color-scheme: dark) {
      .device-screen {
        background: #1a1a1a;
      }

      .screen-content h3,
      .features li {
        color: #e0e0e0;
      }

      .screen-content p {
        color: #a0a0a0;
      }

      .breakpoint-card {
        background: #2d2d2d;
        border-color: #444;
      }

      .breakpoint-content p {
        color: #a0a0a0;
      }

      .breakpoint-content strong {
        color: #e0e0e0;
      }
    }
  </style>
</BaseLayout>
