# 📚 项目完整文档

## 项目概述

这是一个使用 **Astro** 构建的现代化个人博客系统，提供高性能、易于定制的博客、笔记和文档功能。

### 核心特性

- ⚡ **极速性能** - Astro 默认零 JavaScript，首屏加载极快
- 📝 **灵活内容管理** - 博客、学习笔记、项目文档一站式管理
- 🎨 **现代化设计** - 简洁优雅的响应式布局
- 📁 **清晰的目录结构** - 易于维护和扩展
- 🔧 **模块化组件** - 可复用的 Astro 组件系统
- 💾 **Markdown 驱动** - 内容用 Markdown 编写，支持 YAML Frontmatter

---

## 📋 目录导航

1. [项目结构](#项目结构)
2. [技术栈](#技术栈)
3. [页面说明](#页面说明)
4. [内容管理](#内容管理)
5. [组件系统](#组件系统)
6. [样式系统](#样式系统)
7. [工具函数](#工具函数)
8. [快速开始](#快速开始)
9. [开发指南](#开发指南)
10. [扩展和定制](#扩展和定制)

---

## 项目结构

```
my_blog/
├── src/
│   ├── components/              ✨ 可复用的 Astro 组件
│   │   ├── ItemLink.astro       # 链接项组件 - 用于列表项显示
│   │   ├── Breadcrumb.astro     # 面包屑导航 - 用于路径导航
│   │   └── Card.astro           # 卡片组件 - 通用卡片容器
│   │
│   ├── layouts/                 📐 页面布局模板
│   │   └── BaseLayout.astro     # 基础布局 - 包含导航、页脚、全局样式
│   │
│   ├── pages/                   🔗 路由页面（自动转换为 URL）
│   │   ├── index.astro          # 首页 (/)
│   │   │   └─ 特点：五彩缤纷的首页设计，展示项目特点和快速导航
│   │   │
│   │   ├── blog.astro           # 博客列表 (/blog)
│   │   │   └─ 特点：显示所有博客文章，支持标签、作者、日期筛选
│   │   │
│   │   ├── blog/
│   │   │   └── [slug].astro     # 博客文章详情 (/blog/{slug})
│   │   │       └─ 特点：动态生成文章页面，支持 Markdown 渲染
│   │   │
│   │   ├── notes.astro          # 笔记首页 (/notes)
│   │   │   └─ 特点：显示所有笔记分类列表
│   │   │
│   │   ├── notes/
│   │   │   ├── [category].astro # 分类页面 (/notes/{category})
│   │   │   │   └─ 特点：显示分类下的所有笔记文件
│   │   │   │
│   │   │   └── [category]/[slug].astro  # 笔记详情 (/notes/{category}/{slug})
│   │   │       └─ 特点：渲染笔记内容，支持复杂 Markdown 格式
│   │   │
│   │   ├── docs.astro           # 文档页面 (/docs)
│   │   │   └─ 特点：项目开发文档和使用指南
│   │   │
│   │   └── [其他页面]           # 可添加更多页面
│   │
│   ├── content/                 📚 内容集合（Markdown 文件）
│   │   ├── blog/                # 博客文章目录
│   │   │   ├── 01-welcome.md    # 示例文章
│   │   │   ├── hello-world.md   # 示例文章
│   │   │   └── [*.md]           # 更多文章文件
│   │   │
│   │   ├── notes/               # 笔记按分类组织
│   │   │   ├── 高等数学/
│   │   │   │   ├── 傅里叶变换.md
│   │   │   │   └── [*.md]
│   │   │   ├── 线性代数/
│   │   │   ├── 编程/
│   │   │   └── [其他分类]/
│   │   │
│   │   └── config.ts            # Content Collections 配置文件
│   │       └─ 定义：blog 和 notes 集合的数据模式
│   │
│   ├── styles/                  🎨 全局样式
│   │   └── global.css           # 全局 CSS - 包含 CSS 变量和全局样式
│   │       └─ 内容：颜色变量、字体设置、重置样式、标准元素样式
│   │
│   ├── utils/                   🔧 工具函数库
│   │   └── fs.ts                # 文件系统工具
│   │       └─ 包含：目录读取、文件获取、Frontmatter 提取等函数
│   │
│   └── assets/                  🖼️ 静态资源（图片、字体等）
│
├── public/                      📦 公开资源（直接复制到输出）
│
├── astro.config.mjs             ⚙️ Astro 配置文件
├── tsconfig.json                📝 TypeScript 配置
├── package.json                 📦 项目依赖配置
├── .gitignore                   🚫 Git 忽略文件列表
├── README.md                    📖 项目自述文件
└── PROJECT.md                   📋 **本文件** - 项目完整文档
```

### 目录职责一览表

| 目录 | 职责 | 文件类型 | 说明 |
|------|------|--------|------|
| `components/` | 可复用组件库 | `.astro` | 存放页面间共享的组件 |
| `layouts/` | 页面布局模板 | `.astro` | 定义页面框架结构 |
| `pages/` | 路由和页面 | `.astro` | 文件名自动转换为 URL |
| `content/` | 内容数据库 | `.md` | Markdown 格式的内容 |
| `styles/` | 样式文件 | `.css` | 全局和局部样式 |
| `utils/` | 工具函数 | `.ts` | 可复用的逻辑函数 |
| `assets/` | 静态资源 | 各种格式 | 图片、字体等文件 |
| `public/` | 公开文件 | 各种格式 | 直接输出到根目录 |

---

## 技术栈

### 核心框架

- **Astro 5.x** - 现代化的静态网站生成器
  - 零 JavaScript 优先：默认不发送 JS 到浏览器
  - 岛屿架构：只为交互部分发送 JS
  - 内容驱动：天生支持 Markdown

### 内容格式

- **Markdown** - 内容编写格式
- **YAML Frontmatter** - 文章元数据
  ```yaml
  ---
  title: 文章标题
  description: 文章描述
  date: 2026-01-15
  tags: [标签1, 标签2]
  ---
  ```

### 样式技术

- **CSS 3** - 原生 CSS 没有额外预处理器
- **CSS Variables** - 全局色彩管理
- **Responsive Design** - 移动优先的响应式设计

### 开发工具

- **TypeScript** - 类型安全的 JavaScript
- **marked** - Markdown 解析库
- **Node.js** - 运行环境

### 依赖包

```json
{
  "dependencies": {
    "astro": "^5.16.9",
    "marked": "^latest"
  }
}
```

---

## 页面说明

### 1. 首页 (`/`)

**文件位置**：`src/pages/index.astro`

**功能职责**：
- 项目首页和介绍
- 展示项目核心特性
- 提供快速导航入口

**页面结构**：
```
┌─────────────────────────────────────┐
│           Hero 英雄区块              │
│   欢迎来到我的博客 (大标题)          │
│  子标题和项目描述                   │
└─────────────────────────────────────┘

┌─────────────────────────────────────┐
│      功能特性卡片区块 (4 列)         │
│ 🚀 高性能 │ 📝 灵活 │ 🎨 设计 │ 🔧  │
└─────────────────────────────────────┘

┌─────────────────────────────────────┐
│        快速链接导航卡片              │
│  📚 博客  │  📋 笔记  │  📖 文档    │
└─────────────────────────────────────┘

┌─────────────────────────────────────┐
│          行动号召区块 (CTA)          │
│  提供两个主要操作按钮                │
└─────────────────────────────────────┘
```

**关键元素**：
- Hero 英雄部分：渐变标题吸引注意力
- 特性卡片：网格布局，悬停效果
- 快速链接：大图标，易点击
- 操作按钮：主按钮和次按钮

**技术特点**：
- 使用 CSS 渐变实现标题效果
- 响应式网格布局（自适应列数）
- 平滑的过渡和悬停效果

---

### 2. 博客列表页 (`/blog`)

**文件位置**：`src/pages/blog.astro`

**功能职责**：
- 显示所有博客文章列表
- 文章按发布日期倒序排列
- 显示文章元数据（标题、日期、作者、标签）
- 提供文章卡片跳转链接

**页面结构**：
```
┌─────────────────────────────────────┐
│        页面标题和副标题              │
│        博客文章 | 记录心得           │
└─────────────────────────────────────┘

┌─────────────────────────────────────┐
│   📄 文章卡片 (可点击，多个)        │
│  ┌──────────────────────────────┐  │
│  │ 文章标题                      │  │
│  │ 2026-01-15 • 作者名 • 3 个标签 │  │
│  │ 文章描述摘要...               │  │
│  │ 🏷️ tag1  🏷️ tag2  🏷️ tag3   │  │
│  │ 阅读全文 →                    │  │
│  └──────────────────────────────┘  │
│                                     │
│  ┌──────────────────────────────┐  │
│  │ [更多文章卡片...]             │  │
│  └──────────────────────────────┘  │
└─────────────────────────────────────┘

[空文章状态] - 暂时还没有文章
```

**关键特性**：
- 文章按最新优先排列
- 卡片设计美观，悬停提升效果
- 完整的元数据展示
- 响应式布局，网格自适应

**技术实现**：
```typescript
// 按发布日期倒序排列
const posts = (await getCollection('blog')).sort(
  (a, b) => b.data.pubDate.valueOf() - a.data.pubDate.valueOf()
);
```

---

### 3. 博客文章详情页 (`/blog/[slug]`)

**文件位置**：`src/pages/blog/[slug].astro`

**功能职责**：
- 显示完整的博客文章
- 渲染 Markdown 内容为 HTML
- 显示文章元数据
- 提供文章返回链接

**页面结构**：
```
[ ← 返回博客 ]

标题
发布日期 • 作者

[正文内容 - Markdown 渲染的 HTML]

- 支持所有 Markdown 语法
- 代码块、列表、表格、图片等
- 自定义样式和排版
```

**支持的 Markdown 特性**：
- 标题 (H1-H6)
- 列表 (有序/无序)
- 代码块和行内代码
- 表格
- 块引用
- 图片
- 链接
- 加粗、斜体等文本格式

---

### 4. 笔记首页 (`/notes`)

**文件位置**：`src/pages/notes.astro`

**功能职责**：
- 显示所有笔记分类
- 分类文件夹列表
- 快速访问各个分类

**页面结构**：
```
┌─────────────────────────────────────┐
│        页面标题和副标题              │
│     学习笔记 | 知识点汇总            │
└─────────────────────────────────────┘

┌─────────────────────────────────────┐
│  分类项链接 (可点击，列表)           │
│  ┌──────────────────────────────┐  │
│  │ 📁 高等数学            →      │  │
│  ├──────────────────────────────┤  │
│  │ 📁 线性代数            →      │  │
│  ├──────────────────────────────┤  │
│  │ 📁 编程                →      │  │
│  ├──────────────────────────────┤  │
│  │ 📁 [其他分类]          →      │  │
│  └──────────────────────────────┘  │
└─────────────────────────────────────┘

[空状态] - 暂时还没有笔记
```

**特性**：
- 按字母顺序排列分类
- 使用 ItemLink 组件统一风格
- 支持快速导航

---

### 5. 笔记分类页 (`/notes/[category]`)

**文件位置**：`src/pages/notes/[category].astro`

**功能职责**：
- 显示某个分类下的所有笔记文件
- 动态生成分类页面
- 提供面包屑导航

**页面结构**：
```
[笔记 / 高等数学]  ← 面包屑导航

高等数学

┌─────────────────────────────────────┐
│  文件项链接 (可点击，列表)           │
│  ┌──────────────────────────────┐  │
│  │ 📄 傅里叶变换          →      │  │
│  ├──────────────────────────────┤  │
│  │ 📄 微积分基础          →      │  │
│  ├──────────────────────────────┤  │
│  │ 📄 [其他笔记]          →      │  │
│  └──────────────────────────────┘  │
└─────────────────────────────────────┘
```

**特性**：
- 面包屑导航清晰
- 文件列表有序
- 点击进入详情页

---

### 6. 笔记详情页 (`/notes/[category]/[slug]`)

**文件位置**：`src/pages/notes/[category]/[slug].astro`

**功能职责**：
- 显示笔记的完整内容
- 渲染 Markdown
- 三层面包屑导航
- 支持复杂内容格式

**页面结构**：
```
[笔记 / 高等数学 / 傅里叶变换]  ← 三层面包屑

# 傅里叶变换

[正文内容 - Markdown 渲染]
- 所有标准 Markdown 语法
- 代码、表格、图片等
- 专业排版和样式
```

**特性**：
- 详细的面包屑导航
- 完整的 Markdown 支持
- 内容排版优化

---

### 7. 文档页 (`/docs`)

**文件位置**：`src/pages/docs.astro`

**功能职责**：
- 项目开发文档和指南
- 快速参考
- 技术说明

**包含内容**：
- 项目结构概览
- 快速开始指南
- 核心概念说明
- 使用说明和示例
- 自定义指南
- 学习资源链接

---

## 内容管理

### 博客文章

**位置**：`src/content/blog/`

**文件格式**：Markdown 文件 (`.md`)

**Frontmatter 模板**：
```yaml
---
title: '文章标题'                    # 必需
description: '文章简短描述'          # 可选
author: '作者名称'                   # 可选
date: 2026-01-15                    # 可选 (使用此字段或 pubDate)
pubDate: 2026-01-15                 # 可选 (使用此字段或 date)
tags: ['标签1', '标签2']            # 可选
draft: false                        # 可选 (标记为草稿)
---

# 文章内容

正文用 Markdown 格式编写...
```

**数据模式**（在 `config.ts` 中定义）：
```typescript
const blog = defineCollection({
  type: 'content',
  schema: z.object({
    title: z.string(),
    description: z.string().optional(),
    author: z.string().optional(),
    pubDate: z.coerce.date().optional(),
    date: z.coerce.date().optional(),
    updatedDate: z.coerce.date().optional(),
    tags: z.array(z.string()).optional(),
    categories: z.array(z.string()).optional(),
    draft: z.boolean().optional(),
  }).transform((data) => ({
    ...data,
    pubDate: data.pubDate || data.date || new Date(),
  })),
});
```

**查询方法**：
```typescript
import { getCollection } from 'astro:content';

// 获取所有文章
const posts = await getCollection('blog');

// 排序和过滤
const sortedPosts = posts
  .filter(post => !post.data.draft)
  .sort((a, b) => b.data.pubDate.valueOf() - a.data.pubDate.valueOf());
```

### 学习笔记

**位置**：`src/content/notes/`

**目录结构**：按学科或主题分类
```
notes/
├── 高等数学/
│   ├── 傅里叶变换.md
│   ├── 微积分基础.md
│   └── [*.md]
├── 线性代数/
│   ├── 矩阵论.md
│   └── [*.md]
├── 编程/
│   ├── Python 基础.md
│   └── [*.md]
└── [其他分类]/
```

**Frontmatter 模板**：
```yaml
---
title: '笔记标题'  # 可选，如果不填会使用文件名
---

# 笔记内容

用 Markdown 格式编写...
```

**访问方式**：
- 首页：`/notes`
- 分类：`/notes/{category}`
- 笔记：`/notes/{category}/{slug}`

---

## 组件系统

### 1. BaseLayout 组件

**位置**：`src/layouts/BaseLayout.astro`

**职责**：页面基础布局模板

**特点**：
- 包含导航栏、页脚
- 导入全局样式
- 响应式设计

**使用方法**：
```astro
---
import BaseLayout from '../layouts/BaseLayout.astro';
---

<BaseLayout title="页面标题" description="页面描述">
  <!-- 页面内容 -->
</BaseLayout>
```

### 2. ItemLink 组件

**位置**：`src/components/ItemLink.astro`

**职责**：可点击的列表项组件

**Props**：
```typescript
interface Props {
  href: string;          // 链接地址
  icon?: string;         // 图标 emoji (默认 📄)
  title: string;         // 标题文本
}
```

**使用示例**：
```astro
<ItemLink
  href="/notes/高等数学"
  icon="📁"
  title="高等数学"
/>
```

**特点**：
- 统一的样式
- 悬停提升效果
- 支持自定义图标

### 3. Breadcrumb 组件

**位置**：`src/components/Breadcrumb.astro`

**职责**：面包屑导航组件

**Props**：
```typescript
interface Props {
  items: Array<{
    label: string;
    href: string;
  }>;
}
```

**使用示例**：
```astro
<Breadcrumb items={[
  { label: '首页', href: '/' },
  { label: '笔记', href: '/notes' },
  { label: '高等数学', href: '/notes/高等数学' }
]} />
```

### 4. Card 组件

**位置**：`src/components/Card.astro`

**职责**：通用卡片容器

**Props**：
```typescript
interface Props {
  title: string;
  description?: string;
}
```

**使用示例**：
```astro
<Card title="卡片标题" description="描述文本">
  卡片内容
</Card>
```

---

## 样式系统

### 全局样式文件

**位置**：`src/styles/global.css`

### CSS 变量

所有颜色都通过 CSS 变量管理：

```css
:root {
  /* 主题颜色 */
  --color-primary: #0066cc;           /* 主颜色 - 蓝色 */
  --color-primary-hover: #0052a3;     /* 主颜色悬停 - 深蓝 */
  
  /* 背景颜色 */
  --color-bg-light: #fafafa;          /* 浅灰背景 */
  --color-bg-lighter: #f5f5f5;        /* 更浅灰背景 */
  
  /* 边框颜色 */
  --color-border: #eee;               /* 浅边框 */
  --color-border-dark: #ddd;          /* 深边框 */
  
  /* 文本颜色 */
  --color-text-primary: #333;         /* 主文本 - 深灰 */
  --color-text-secondary: #666;       /* 次文本 - 中灰 */
  --color-text-muted: #999;           /* 灰色文本 */
  
  /* 强调颜色 */
  --color-accent: #d63384;            /* 强调色 - 紫红 */
}
```

### 全局重置和标准

- 盒模型：`box-sizing: border-box`
- 字体：系统字体栈
- 行高：`1.6`（正文），`1.3`（标题）
- 选中效果：主颜色背景 + 白色文字

### 元素样式

标准化的元素样式：
- **链接**：主颜色，悬停变深
- **标题**：不同字号和样式
- **列表**：有序/无序列表
- **代码**：行内代码和代码块
- **表格**：完整的表格样式
- **块引用**：左边框 + 斜体
- **图片**：自适应宽度 + 圆角

### 响应式设计

- 流体排版：使用 rem 单位
- 网格布局：`grid-template-columns: repeat(auto-fit, minmax(...))`
- 弹性布局：`flexbox` 处理导航和布局
- 断点：使用 CSS 媒体查询处理不同屏幕

### 自定义样式

在组件或页面的 `<style>` 块中定义本地样式：

```astro
<style>
  /* 这些样式仅作用于当前组件 */
  .my-class {
    color: var(--color-primary);
  }
</style>
```

---

## 工具函数

### 位置

`src/utils/fs.ts`

### 可用函数

#### 1. `getDirectoryItems(dirPath: string)`

获取目录中的所有项目（文件和文件夹）。

**参数**：`dirPath` - 目录路径

**返回**：`CategoryItem[]` 数组

**示例**：
```typescript
import { getDirectoryItems } from '../utils/fs';

const items = await getDirectoryItems('./src/content/notes');
```

#### 2. `getCategoryFiles(categoryPath: string)`

获取分类目录下的所有 Markdown 文件。

**参数**：`categoryPath` - 分类路径

**返回**：`FileItem[]` 数组

**示例**：
```typescript
const files = await getCategoryFiles('./src/content/notes/高等数学');
```

#### 3. `extractTitleFromFrontmatter(fileContent: string)`

从 Markdown 内容中提取标题。

**参数**：`fileContent` - 完整的 Markdown 内容

**返回**：标题字符串

**示例**：
```typescript
const title = extractTitleFromFrontmatter(mdContent);
```

#### 4. `extractMarkdownContent(fileContent: string)`

去除 Frontmatter，获取 Markdown 正文。

**参数**：`fileContent` - 完整的 Markdown 内容

**返回**：去除 Frontmatter 后的内容

**示例**：
```typescript
const markdown = extractMarkdownContent(mdContent);
```

#### 5. `slugify(text: string)`

将中文文本转换为 URL-safe 的 slug。

**参数**：`text` - 文本

**返回**：转换后的 slug

**示例**：
```typescript
slugify('高等数学') // => '高等数学'
```

---

## 快速开始

### 1. 安装依赖

```bash
npm install
```

### 2. 启动开发服务器

```bash
npm run dev
```

访问 `http://localhost:4321`

### 3. 创建第一篇博客文章

在 `src/content/blog/` 创建 `my-first-post.md`：

```markdown
---
title: '我的第一篇文章'
description: '这是我的第一篇博客文章'
author: '你的名字'
date: 2026-01-15
tags: ['Astro', '博客']
---

# 我的第一篇文章

这是文章的内容...
```

### 4. 创建第一个笔记分类和文件

创建目录：`src/content/notes/我的学科/`

创建文件：`src/content/notes/我的学科/笔记标题.md`

```markdown
---
title: '笔记标题'
---

# 笔记标题

笔记内容...
```

### 5. 构建和部署

**构建生产版本**：
```bash
npm run build
```

**预览生产构建**：
```bash
npm run preview
```

**部署到 Vercel**（推荐）：
```bash
npm install -g vercel
vercel
```

---

## 开发指南

### 添加新页面

1. 在 `src/pages/` 创建 `.astro` 文件
2. 导入 `BaseLayout`
3. 编写内容

**示例**：`src/pages/about.astro`
```astro
---
import BaseLayout from '../layouts/BaseLayout.astro';
---

<BaseLayout title="关于我">
  <article>
    <h1>关于我</h1>
    <p>自我介绍...</p>
  </article>
</BaseLayout>
```

### 创建新组件

1. 在 `src/components/` 创建 `.astro` 文件
2. 定义 Props 接口
3. 导入使用

**示例**：`src/components/MyComponent.astro`
```astro
---
interface Props {
  title: string;
  content: string;
}

const { title, content } = Astro.props;
---

<div class="my-component">
  <h2>{title}</h2>
  <p>{content}</p>
</div>

<style>
  .my-component {
    /* 样式 */
  }
</style>
```

### 添加新工具函数

在 `src/utils/fs.ts` 中添加函数：

```typescript
/**
 * 函数描述
 * @param param 参数说明
 * @returns 返回值说明
 */
export function myFunction(param: string): string {
  // 实现逻辑
  return result;
}
```

### 修改全局样式

编辑 `src/styles/global.css`：

```css
/* 修改 CSS 变量 */
:root {
  --color-primary: #your-color;
}

/* 添加全局样式 */
body {
  /* 新样式 */
}
```

### 使用组件

在页面或其他组件中：

```astro
---
import MyComponent from '../components/MyComponent.astro';
---

<MyComponent title="标题" content="内容" />
```

---

## 扩展和定制

### 添加框架支持

```bash
npm run astro add react      # 添加 React
npm run astro add vue        # 添加 Vue
npm run astro add svelte     # 添加 Svelte
npm run astro add tailwind   # 添加 Tailwind CSS
```

### 使用 React 组件

安装后在 `.astro` 文件中使用：

```astro
---
import MyReactComponent from '../components/MyReactComponent.jsx';
---

<MyReactComponent client:load />
```

### 添加自定义字体

在 `src/styles/global.css`：

```css
@import url('https://fonts.googleapis.com/css2?family=Noto+Sans+SC&display=swap');

body {
  font-family: 'Noto Sans SC', sans-serif;
}
```

### 修改导航链接

编辑 `src/layouts/BaseLayout.astro`：

```astro
<nav class="nav">
  <a href="/">首页</a>
  <a href="/blog">博客</a>
  <a href="/notes">笔记</a>
  <a href="/docs">文档</a>
  <a href="/about">关于</a>  <!-- 添加新链接 -->
</nav>
```

### 添加社交媒体链接

在页脚添加链接：

```astro
<footer>
  <div class="social-links">
    <a href="https://twitter.com/..." target="_blank">Twitter</a>
    <a href="https://github.com/..." target="_blank">GitHub</a>
  </div>
</footer>
```

### 自定义配置

编辑 `astro.config.mjs` 修改项目配置：

```javascript
export default defineConfig({
  site: 'https://example.com',
  integrations: [/* 集成 */],
  // ... 更多配置
});
```

---

## 常见问题

### Q: 如何添加评论功能？

A: 可以集成第三方评论服务，如 Disqus 或 Giscus。

### Q: 如何支持更多语言？

A: 使用 Astro 的国际化 (i18n) 特性。参考 [Astro i18n 指南](https://docs.astro.build/en/guides/internationalization/)。

### Q: 如何优化图片加载？

A: 使用 Astro 的 `<Image>` 组件自动优化图片。

### Q: 如何添加搜索功能？

A: 可以集成 Algolia 或使用客户端搜索库。

### Q: 如何自定义 URL 结构？

A: 使用 Astro 的路由配置和动态路由。

---

## 有用的资源

### 官方文档
- [Astro 官方文档](https://docs.astro.build)
- [Astro 中文文档](https://docs.astro.build/zh-cn/)
- [Markdown 语法指南](https://www.markdownguide.org/)

### 学习资源
- [Astro 教程](https://docs.astro.build/zh-cn/tutorial/0-introduction/)
- [Web 性能优化](https://web.dev/performance/)
- [CSS 变量完全指南](https://css-tricks.com/a-complete-guide-to-custom-properties/)

### 相关工具
- [Vercel - 部署平台](https://vercel.com)
- [Netlify - 部署平台](https://netlify.com)
- [GitHub Pages - 免费托管](https://pages.github.com)

---

## 更新日志

### v1.0.0 (2026-01-15)

**初始版本发布**

✅ 完成功能：
- 博客文章系统
- 学习笔记管理
- 项目文档
- 响应式设计
- CSS 变量系统
- 工具函数库
- 完整项目文档

---

## 许可证

MIT License - 自由使用和修改

---

## 联系方式

如有问题或建议，欢迎反馈！

---

**最后更新**：2026-01-15  
**文档版本**：1.0.0
