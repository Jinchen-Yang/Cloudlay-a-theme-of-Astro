# 🎨 首页设计使用指南

## 快速概览

本博客采用现代化的响应式设计，灵感源自 `astro-koharu` 主题。首页包含：

1. **左侧侧边栏**（桌面）/ **顶部栏**（手机）- 作者信息卡片
2. **中部主要区域** - 欢迎部分 + 文章列表
3. **完全响应式** - 手机、平板、桌面完美适配

---

## 📱 首页布局

### 桌面版本 (> 1024px)

```
┌─────────────────────────────────────────────────┐
│ 导航栏                                          │
├────────────────┬────────────────────────────────┤
│  作者卡片      │  欢迎区域                      │
│  · 头像        │  ✨ 欢迎来到我的博客          │
│  · 基本信息    │                                │
│  · 社交链接    │  最新文章                     │
│  · 统计数据    │  ┌─────────────────────────┐ │
│  · 快速导航    │  │ 📝 文章卡片1            │ │
│                │  │ 标题 + 摘要             │ │
│                │  │ 继续阅读 →              │ │
│                │  └─────────────────────────┘ │
│                │  ┌─────────────────────────┐ │
│                │  │ 📝 文章卡片2            │ │
│                │  └─────────────────────────┘ │
│                │  ...                        │
└────────────────┴────────────────────────────────┘
```

### 平板版本 (768px - 1024px)

```
┌──────────────────────────────────┐
│ 导航栏                           │
├──────────────┬──────────────────┤
│ 作者卡片     │ 欢迎区域         │
│ (网格模式)   │ 文章列表         │
│              │                  │
└──────────────┴──────────────────┘
```

### 手机版本 (< 768px)

```
┌──────────────────────────────────┐
│ 导航栏                           │
├──────────────────────────────────┤
│ 作者卡片                         │
│ · 头像                           │
│ · 信息                           │
│ · 导航                           │
│ · 统计                           │
├──────────────────────────────────┤
│ 欢迎区域                         │
├──────────────────────────────────┤
│ 文章列表                         │
│ · 文章卡片 1                     │
│ · 文章卡片 2                     │
│ ...                              │
└──────────────────────────────────┘
```

---

## 🧩 组件说明

### 1. AuthorCard 组件

**位置**: `src/components/AuthorCard.astro`

**用途**: 展示作者/博客主人的信息卡片

**功能**:
- 显示头像（带悬停效果）
- 显示作者名、职位、标语
- 显示社交媒体链接
- 显示统计数据（文章数、分类数、标签数）
- 快速导航按钮

**Props**：

```typescript
interface Props {
  name: string;              // 作者名字
  title: string;            // 职位/头衔
  avatar: string;           // 头像URL
  bio: string;              // 个人简介
  tagline: string;          // 标语
  email: string;            // 邮箱（用于联系按钮）
  github?: string;          // GitHub用户名
  twitter?: string;         // Twitter用户名
  website?: string;         // 个人网站URL
  stats?: {                 // 统计数据
    posts?: number;         // 文章数
    categories?: number;    // 分类数
    tags?: number;          // 标签数
  };
}
```

**使用示例**：

```astro
---
import AuthorCard from '../components/AuthorCard.astro';
---

<AuthorCard
  name="Jinchen Yang"
  title="全栈开发者"
  avatar="/avatar.jpg"
  bio="热爱技术的开发者，专注于 Web 开发、自动化和系统架构。"
  tagline="一个关于代码、学习和生活的博客"
  email="jinchen@example.com"
  github="Jinchen-Yang"
  twitter="jinchen_yang"
  website="https://example.com"
  stats={{
    posts: 10,
    categories: 5,
    tags: 20
  }}
/>
```

**自定义方式**：

1. **修改作者信息** - 编辑 `src/pages/index.astro` 中的 `authorInfo` 对象
2. **修改颜色** - 修改 `src/styles/global.css` 中的CSS变量
3. **修改图片** - 替换 `/public/avatar.jpg`

---

### 2. PostCard 组件

**位置**: `src/components/PostCard.astro`

**用途**: 展示单个博客文章的卡片

**功能**:
- 显示文章标题
- 显示发布日期
- 显示作者名
- 显示文章摘要/描述
- 显示标签
- 悬停效果

**Props**：

```typescript
interface Props {
  title: string;           // 文章标题
  description?: string;    // 文章描述/摘要
  slug: string;           // 文章URL slug
  date?: Date;            // 发布日期
  author?: string;        // 作者名
  tags?: string[];        // 标签数组
}
```

**使用示例**：

```astro
---
import PostCard from '../components/PostCard.astro';
---

<PostCard
  title="Astro 性能优化指南"
  description="探索如何优化 Astro 博客的加载速度和性能"
  slug="astro-optimization-guide"
  date={new Date('2026-01-15')}
  author="Jinchen Yang"
  tags={['Astro', '性能', '优化']}
/>
```

**在首页使用**：

```astro
{posts.length === 0 ? (
  <div class="empty-state">...</div>
) : (
  posts.map((post) => (
    <PostCard
      title={post.data.title}
      description={post.data.description}
      slug={post.slug}
      date={post.data.date}
      author={post.data.author}
      tags={post.data.tags}
    />
  ))
)}
```

---

## 🎨 自定义设计

### 修改颜色主题

编辑 `src/pages/index.astro` 中的CSS变量：

```css
:root {
  /* 主色 - 改为你喜欢的颜色 */
  --color-primary: #667eea;
  --color-primary-light: #f0f4ff;
  --color-primary-hover: #5568d3;
  
  /* 辅色 */
  --color-secondary: #764ba2;
  
  /* 背景和文本 */
  --color-bg: #ffffff;
  --color-bg-secondary: #f8f9fa;
  --color-text-primary: #1a202c;
  --color-text-secondary: #718096;
}
```

### 修改作者信息

编辑 `src/pages/index.astro`：

```astro
---
const authorInfo = {
  name: "你的名字",           // 改为你的名字
  title: "你的职位",          // 改为你的职位
  avatar: "/avatar.jpg",      // 改为你的头像路径
  bio: "你的简介",            // 改为你的简介
  tagline: "你的标语",        // 改为你的标语
  location: "你的位置",       // 改为你的位置
  email: "你的邮箱",          // 改为你的邮箱
  github: "你的GitHub用户名", // 改为你的GitHub用户名
  twitter: "你的Twitter用户名" // 改为你的Twitter用户名
};
---
```

### 修改统计数据

编辑 `src/pages/index.astro`：

```astro
---
const stats = {
  posts: 10,       // 文章数
  categories: 5,   // 分类数
  tags: 20         // 标签数
};
---
```

### 修改快速链接

编辑 AuthorCard 组件中的快速导航：

```astro
<div class="quick-nav">
  <a href="/blog" class="nav-button primary">📚 浏览文章</a>
  <a href="/notes" class="nav-button secondary">📖 学习笔记</a>
  <!-- 添加更多链接 -->
  <a href="/projects" class="nav-button secondary">🚀 项目</a>
</div>
```

---

## 📱 响应式测试

### 测试清单

```bash
# 开发服务器启动
npm run dev

# 在浏览器中测试不同尺寸
```

#### 手机测试 (320px - 480px)

- [ ] 所有文本可读
- [ ] 按钮可点击（最小44x44px）
- [ ] 没有水平滚动
- [ ] 侧边栏在顶部
- [ ] 图片响应式

#### 平板测试 (768px - 1024px)

- [ ] 两列布局正确
- [ ] 横屏/竖屏切换流畅
- [ ] 侧边栏大小合适
- [ ] 触摸交互有效

#### 桌面测试 (1280px+)

- [ ] 侧边栏粘性定位
- [ ] 内容宽度合理
- [ ] 悬停效果流畅
- [ ] 性能无瓶颈

### 浏览器DevTools测试

1. **打开DevTools**: `F12` 或 `Cmd+Option+I`
2. **切换设备工具栏**: `Ctrl+Shift+M` 或 `Cmd+Shift+M`
3. **选择设备**:
   - iPhone SE (375px)
   - iPhone 12 Pro (390px)
   - iPad Air (820px)
   - Desktop (1920px)
4. **测试功能**:
   - 点击链接和按钮
   - 悬停效果
   - 深色模式切换

---

## 🌙 深色模式支持

首页自动支持系统深色模式，无需额外配置。

### 测试深色模式

**macOS**:
- 系统设置 > 外观 > 深色

**Windows**:
- 设置 > 个性化 > 颜色 > 深色

**浏览器DevTools**:
```javascript
// 在控制台运行
document.documentElement.style.colorScheme = 'dark';
// 或重置为浅色
document.documentElement.style.colorScheme = 'light';
```

---

## ⚡ 性能优化

### 已实现的优化

- ✅ **CSS Grid布局** - 高效的布局引擎
- ✅ **CSS变量** - 减少重复代码
- ✅ **零JavaScript** - 默认无客户端JS
- ✅ **动画优化** - 使用 `transform` 和 `opacity`
- ✅ **图片优化** - 使用placeholder和 `onerror`
- ✅ **缓存友好** - 纯静态文件

### 页面加载性能目标

| 指标 | 目标 | 当前 |
|-----|------|------|
| 首屏加载 | < 1s | < 500ms |
| 可交互 | < 2s | < 1s |
| 总包体积 | < 100KB | < 50KB |
| JavaScript | 0KB | 0KB (默认) |

---

## 🚀 部署

### 生成生产构建

```bash
npm run build
```

### 本地预览构建

```bash
npm run preview
```

### 部署到平台

#### GitHub Pages

```bash
# 在 astro.config.mjs 中配置
export default defineConfig({
  site: 'https://yourusername.github.io',
  base: '/repository-name',
});

# 构建
npm run build

# 推送到 GitHub
git add .
git commit -m "Deploy to GitHub Pages"
git push origin main
```

#### Netlify

```bash
# 连接 GitHub 仓库即可自动部署
# Netlify 会自动检测 astro 项目
```

#### Vercel

```bash
# 安装 Vercel CLI
npm i -g vercel

# 部署
vercel
```

---

## 📚 文件结构

```
my_blog/
├── src/
│   ├── pages/
│   │   ├── index.astro           # 首页（主要入口）
│   │   ├── blog.astro            # 博客列表
│   │   ├── notes.astro           # 笔记首页
│   │   └── ...
│   ├── components/
│   │   ├── AuthorCard.astro      # 作者卡片组件
│   │   ├── PostCard.astro        # 文章卡片组件
│   │   ├── Breadcrumb.astro      # 面包屑导航
│   │   └── ...
│   ├── styles/
│   │   ├── global.css            # 全局样式 + CSS变量
│   │   └── obsidian.css          # Obsidian 主题
│   └── ...
├── public/
│   └── avatar.jpg                # 头像图片
├── README.md                      # 项目说明
├── RESPONSIVE_DESIGN.md          # 响应式设计文档（本文件）
└── ...
```

---

## 🐛 常见问题

### Q: 如何修改头像?

A: 将你的头像图片放在 `/public/avatar.jpg`，然后在 `src/pages/index.astro` 中修改 `avatar` 路径。

### Q: 如何添加更多社交媒体?

A: 在 AuthorCard 组件中添加新的社交链接元素。

### Q: 如何修改文章卡片的样式?

A: 编辑 `src/components/PostCard.astro` 中的 `<style>` 块。

### Q: 手机端显示不正常?

A: 检查浏览器缩放是否正常，尝试清除缓存或在隐身模式下打开。

### Q: 如何禁用深色模式?

A: 在 `BaseLayout.astro` 的 `<html>` 标签上添加 `class="light"`。

---

## 📞 支持

遇到问题？

1. 查看 [README.md](README.md)
2. 查看 [RESPONSIVE_DESIGN.md](RESPONSIVE_DESIGN.md)
3. 提交 Issue 到 GitHub
4. 查看 [astro-koharu](https://github.com/cosZone/astro-koharu) 的示例代码

---

## 📄 许可证

MIT License - 自由使用、修改和分发

---

**最后更新**: 2026-01-15 | **版本**: 1.0
